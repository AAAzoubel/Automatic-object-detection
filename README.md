# Automatic-object-detection
End-to-end video processing pipeline using YOLOv8, FastAPI, and OpenCV. Performs object detection and tracking across frames, generating structured data (JSON, CSV, Excel) and annotated video outputs for analysis.
